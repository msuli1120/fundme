import { CommentOrderByPipe } from './comment-order-by.pipe';

describe('CommentOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
